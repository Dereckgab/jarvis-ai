# AGENTS.md — AI Coding Agent Instructions

Purpose: Provide concise, actionable guidance so an AI coding agent can be productive in this repository.

What to run (quick):
- Build: `go build ./cmd/main.go`
- Test: `go test ./...`
- Run locally: `go run ./cmd/main.go`
- Docker: `docker build -t jarvis-backend .` then run the container

Architecture (short):
- Entry: [cmd/main.go](cmd/main.go#L1)
- DI / App bootstrap: [internal/app/app.go](internal/app/app.go#L1)
- HTTP layer: [internal/interfaces/http/routes.go](internal/interfaces/http/routes.go#L1)
- Application services: [internal/application/service](internal/application/service#L1)
- Domain entities & repositories: [internal/domain](internal/domain#L1)
- Infrastructure (DB/cache/AI): [internal/infrastructure](internal/infrastructure#L1)
- Shared utilities: [pkg](pkg#L1)

Agent guidelines (keep concise):
- Minimal edits: Make the smallest, well-tested change. Prefer adding new files over large refactors.
- Link, don't duplicate: If repo has docs, link to them instead of copying.
- Tests first: Add or update unit tests for code changes when possible. Run `go test ./...`.
- Environment: Use env vars defined in [config/config.go](config/config.go#L1) for local runs.
- Respect conventions: Use constructors in `internal/app` for DI and repository interfaces in `internal/domain/repository`.
- Logging & errors: Use `pkg/logger` and `pkg/errors` for consistent observability and error handling.

Key places to inspect for common tasks:
- Adding an API route: [internal/interfaces/http/routes.go](internal/interfaces/http/routes.go#L1) and handler files in the same directory.
- Database changes: [internal/infrastructure/database/mysql.go](internal/infrastructure/database/mysql.go#L1) and GORM repositories in [internal/infrastructure/repository](internal/infrastructure/repository#L1).
- AI integrations: [internal/infrastructure/ai](internal/infrastructure/ai#L1) and service adapters in [internal/application/service/ai_service.go](internal/application/service/ai_service.go#L1).

Quick troubleshooting tips:
- If tests fail, run `go test ./... -v` and inspect failures in `tests/`.
- For DB migrations or schema issues, check repository implementations in `internal/infrastructure/repository`.
- If an external service key is needed (OpenAI, Deepseek), check `config/config.go` for env var names.

If you'd like, I can also:
- Update `.github/copilot-instructions.md` instead, or add a smaller `.github`-scoped file.
- Create targeted skills for frontend, infra, or AI connectors.

--
Generated by an AI agent to help other agents quickly reason about this repository.